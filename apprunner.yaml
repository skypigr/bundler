version: 1.0
runtime: nodejs16
build:
  commands:
    pre-build:
      # - yarn run gen-config
    build:
      - yarn && yarn preprocess
    post-build:
      # - yarn run gen-config
  env:
    # - name: BUNDLER_ENV_PORT
    #   value: 3000
run:
  runtime-version: 16.19.0
  command: yarn run bundler --unsafe
  network:
    port: 3000
    env: APP_PORT
  env:
    - name: ENV
      value: "PROD"